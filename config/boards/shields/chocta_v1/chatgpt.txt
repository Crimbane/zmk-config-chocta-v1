&spi1 {
    compatible = "nordic,nrf-spi";
    status = "okay";
    sck-pin = <&pro_micro 9>;   // Pro Micro D9 -> P1.06 for SCK
    mosi-pin = <&pro_micro 8>;  // Pro Micro D8 -> P1.04 for MOSI
    miso-pin = <255>;           // No MISO
    cs-gpios = <&pro_micro 1 GPIO_ACTIVE_LOW>;  // Pro Micro D1 -> P0.06 for CS
    label = "SPI_1";

    oled128x64: oled@0 {
        compatible = "ssd1306";
        reg = <0>;
        spi-max-frequency = <10000000>;
        label = "OLED";
        // Optional pins, define if your OLED uses these:
        // dc-gpios = <&pro_micro 7 GPIO_ACTIVE_LOW>;  // Pro Micro D7 (if used)
        // rst-gpios = <&pro_micro 6 GPIO_ACTIVE_LOW>; // Pro Micro D6 (if used)
    };
};
